
/**
 * Created by sgjeon on 16. 4. 8..
 */
const clc = require('cli-color');

module.exports = {
	log: function(text){
		console.log(clc.white(text));
	},
	warn: function(text){
		console.log(clc.yellow(text));
	},
	error: function(text){
		console.log(clc.red(text));
	}
};