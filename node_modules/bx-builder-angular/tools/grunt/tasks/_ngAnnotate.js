module.exports = function(grunt, options) {

    // 동적 ngAnnotate task
    grunt.registerTask('_ngAnnotate', function(){

        const taskName = 'ngAnnotate';

        const tasks = [taskName];

        grunt.config.set(taskName, require(`../dynamic_config/${taskName}`)(grunt));

        grunt.task.run(tasks);
    });
};